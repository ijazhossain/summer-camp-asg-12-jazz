import { HashLoader } from "react-spinners";

const Spinner = () => {
    return (
        <div className='h-screen flex items-center justify-center'>
            <HashLoader color="#b38b37" />
        </div>
    );
};

export default Spinner;