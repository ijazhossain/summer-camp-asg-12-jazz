const SectionTitle = ({ children }) => {
    return (
        <h1 className="container text-4xl text-center text-[#727475] mt-12 font-semibold"> {children}</h1>
    );
};

export default SectionTitle;